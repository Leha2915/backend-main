[pytest]
pythonpath = .
testpaths = tests
markers =
    llm: tests that require LLM API access (expensive)
    unit: unit tests that don't require external dependencies
    integration: integration tests
    slow: marks tests as slow-running